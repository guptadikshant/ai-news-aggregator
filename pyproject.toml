[project]
name = "ai-news-aggregator"
version = "0.1.0"
description = "An AI-powered news aggregation tool."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "langgraph",
    "openai",
    "python-dotenv",
    "groq",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
    "jupyter>=1.1.1",
]
